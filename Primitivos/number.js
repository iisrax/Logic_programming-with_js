//Números podem ser usados para cálculos, comparações e operações aritméticas.

// Calcular a área de um círculo
let raio = 5;
let area = Math.PI * Math.pow(raio, 2);
console.log(`Área do círculo: ${area}`);

// Comparar números
let x = 10;
let y = 20;
if (x < y) {
  console.log("x é menor que y");
}
